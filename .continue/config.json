{
  "models": [
    {
      "title": "GPT-5 Thinking",
      "provider": "openai",
      "model": "gpt-5-thinking",
      "apiKey": "${OPENAI_API_KEY}"
    }
  ],
  "defaultModel": "GPT-5 Thinking",
  "customCommands": [
    {
      "name": "Scaffold SAS MVP",
      "description": "Generate a Next.js + Tailwind + shadcn site with a futuristic agentic-AI design (blue + accent).",
      "prompt": "Use the following instructions to generate a production-ready Next.js (TypeScript, App Router) site with Tailwind + shadcn/ui and Framer Motion. Create all files, including tailwind.config.ts, postcss.config.js, app/layout.tsx, app/page.tsx, services/page.tsx, process/page.tsx, components (header, footer, hero, section, service-card, process-step, particles-bg, glow-button, logo), and basic SEO. Color scheme: primary blue with a harmonious accent (cyan or purple). Aesthetic: futuristic agentic-AI, soft glows, glassmorphism touches, micro-interactions. Ensure accessibility (WCAG), SEO, and clean TypeScript. Then print the full file tree and each file’s content.\n\nPROMPT TO FOLLOW EXACTLY:\n\n[Paste the 'Prompt: Build SAS MVP (React + Tailwind)' you already gave me here]\n",
      "mode": "generate"
    }
  ]
}
